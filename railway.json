{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "pip install -U langchain-cli ; langchain serve --host \"[::]:$PORT\""
    }
}